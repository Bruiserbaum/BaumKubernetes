# kubectl create secret generic vaultwarden-secret --namespace vaultwarden \
#   --from-literal=ADMIN_TOKEN="$(openssl rand -base64 48)"
apiVersion: v1
kind: Secret
metadata:
  name: vaultwarden-secret
  namespace: vaultwarden
type: Opaque
stringData:
  ADMIN_TOKEN: "REPLACE_WITH_STRONG_TOKEN"
